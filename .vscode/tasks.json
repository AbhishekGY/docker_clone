{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-mydocker",
            "type": "shell",
            "command": "cd ${workspaceFolder} && ./build.sh",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}